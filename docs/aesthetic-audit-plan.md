# Pervagans aesthetic audit (20-agent) — fix plan

Generated 2026-06-04. v89 implemented the token foundation + auth + semantic errors + key polish; the rest are tracked below.

TOTAL FIXES: 36 | sections: 18

=== FIXES (rank | sev | risk | title | files) ===
#1 [high/safe] Add semantic color tokens (error/success/warn) + complete orange/green derivatives + scrim, all mapped to existing hues
     files: apps/web/app/tokens.css
     → In BOTH [data-theme=dark] and [data-theme=light] blocks add: --error/--error-soft/--error-line mapped to the existing --rose triple; --success/--success-soft/--success-line mapped to --green; --warn/--warn-soft/--warn-line mapped to --yellow. Add the missing derivatives: dark --orange-soft:rgba(243,163,102,.10); --orange-line:rgba(243,163,102,.32); --green-soft:rgba(95,212,154,.10); --green-line:rgba(95,212,154,.32); light --orange-soft:rgba(215,122,58,.08); --orange-line:rgba(215,122,58,.28); --green-soft:rgba(31,154,85,.08); --green-line:rgba(31,154,85,.28). Add --scrim: rgba(0,0,0,0.55) (dark) / rgba(15,18,24,0.45) (light). Purely additive — no existing rule changes, so zero regression risk. Unblocks fixes #2,#3,#7,#8,#9.
#2 [high/safe] Fix the data-connected logic-inversion bug on the Connect button
     files: apps/web/app/mcps/RemoteMcpSection.tsx
     → Line 113: remove the bare `data-connected` attribute from the UNCONNECTED (Connect) button. Because mcps.css .connect-btn[data-connected="true"] applies the cyan active style, the Connect and Disconnect buttons currently render identically. Delete the attribute (or set data-connected=false) so only the connected state is highlighted. Verified against source.
#2 [high/moderate] Fix light-mode --cyan failing WCAG AA on every surface (links, CTAs, focus ring)
     files: apps/web/app/tokens.css
     → In [data-theme=light], darken --cyan from #0a8fa6 to ~#0079a0 (≈4.8:1 on white, vs the current 3.8:1 which fails AA for all body-size cyan text — every link, .sb-row active, .auth-foot a, and the global :focus-visible ring at tokens.css L94). Re-confirm it still clears 4.5:1 on --cyan-soft for active chips. Also darken light --green #1f9a55→#157040 and --orange #d77a3a→#a85b15 (both currently fail AA for text). One-surface visual shift in light mode only; dark unchanged.
#3 [high/safe] Route all feature-color active states through tokens (kills light-mode invisible colors + raw-literal duplication)
     files: apps/web/app/dashboard.css, apps/web/app/features/[slug]/feature.css
     → Replace every raw rgba()/rgb() literal in the data-color active rules with the token triples. dashboard.css L128-137 + L1029-1033 (.sb-feature-item[data-color=...]) and feature.css L30-39 + L749 (.feat-shell[data-color=...] --hue-* blocks). Map: yellow→var(--yellow-*), green→var(--green-*), orange→var(--orange-*), indigo→var(--indigo-*), rose→var(--rose-*), fuchsia→var(--fuchsia-*); consolidate the non-palette hues blue→indigo, pink→rose, red→rose, teal→cyan, amber→yellow. Fixes the confirmed bug where light-theme active nav/feature-hero text uses dark-calibrated neon (e.g. raw rgb(74,222,128) at 1.7:1) instead of the theme-aware token, AND removes the two-file duplication. Depends on #1.
#4 [high/safe] Make error surfaces semantically red, not purple — swap all 6+ error boxes + destructive hovers to --error
     files: apps/web/app/(auth)/auth.css, apps/web/app/settings/settings.css, apps/web/app/features/[slug]/feature.css, apps/web/app/mcps/mcps.css, apps/web/app/admin/admin.css, apps/web/app/mcps/RemoteMcpSection.tsx
     → Replace var(--purple)/--purple-soft/--purple-line with var(--error)/--error-soft/--error-line in: .auth-err (auth.css L55-62), .settings-err (settings.css L155-161), .tool-panel .err (mcps.css L377-384), .feat-err (feature.css L64-71), .admin-blocked (admin.css L144-150), .admin-pill.failed/.refunded (admin.css L125-126), .admin-table button.danger (admin.css L109), and the inline error div in RemoteMcpSection.tsx L132. Also switch destructive hovers .sb-history-del:hover/.history-del:hover (dashboard.css L237,L605), .feat-file-del:hover (feature.css L284), and .sb-history-row-menu-item.is-danger / .feat-subsb-row-menu-item.is-danger to --error-*. Disambiguates errors from the purple brand accent and from the Max-tier pill. Depends on #1.
#5 [high/safe] Add the missing SVG icons (search, send, stop, lock, dice) to the icon set
     files: apps/web/app/icons.tsx
     → icons.tsx currently has NO search/send/stop/lock/dice entries (verified — only globe/star/discLogo exist). Add them at strokeWidth 1.8 to match the system: search (circle cx11 cy11 r7 + M21 21l-4.3-4.3), send (M10 14L21 3 M21 3h-7 M21 3v7), stop (<rect x5 y5 width14 height14 rx3 fill=currentColor/>), lock (M8 11V7a4 4 0 0 1 8 0v4 M5 11h14v10H5z), dice (rounded square + 1-3 dots). Prerequisite for fixes #6, #11, #20.
#6 [medium/safe] Unify the icon stroke-weight language to 1.8
     files: apps/web/app/icons.tsx
     → Four icons break the 1.8px contract: change check strokeWidth 2.2→1.8 (L141), dotsV 2→1.8 (L301), trash 1.7→1.8 (L309), and featureSvg() 1.6→1.8 (L330, drives all 8 FEATURE_ICONS shown in sidebar + hero). One file, four edits; immediately stops feature/check/trash icons reading visibly lighter or bolder than their neighbours.
#7 [medium/safe] Replace emoji glyphs and Unicode send/stop chars with the new SVG icons
     files: apps/web/app/features/[slug]/FeatureChat.tsx, apps/web/app/page.tsx, apps/web/app/spaces/[id]/SpaceChat.tsx
     → FeatureChat.tsx L554: replace {seedLocked ? '🔒' : '🎲'} with {seedLocked ? I.lock : I.dice}. Replace the raw send/stop glyphs {sending ? '■' : '↵'} with {sending ? I.stop : I.send} at page.tsx L962, SpaceChat.tsx L433, FeatureChat.tsx L766. Removes the only emoji-as-icon pattern and the inconsistent-weight Unicode arrows. Depends on #5.
#8 [medium/safe] Fix the wrong search-field icon in Spaces (globe → search)
     files: apps/web/app/spaces/page.tsx
     → page.tsx L157: change {I.globe} to {I.search} in .sp-search-ico — the globe (used for web-search elsewhere) is a false affordance on a filter field. Depends on #5 adding I.search.
#9 [high/safe] Promote the auth primary CTA from ghost-tint to a solid, confident button + add hover
     files: apps/web/app/(auth)/auth.css
     → auth.css .auth-btn (L44-53) is currently cyan-soft fill + cyan text + cyan-line border — reads as secondary/selected, not the page's primary action. Change to background: var(--ink); color: var(--bg); border:1px solid transparent; add transition: opacity .15s and :hover:not([disabled]){opacity:.88}. Using ink/bg inverts cleanly in both themes (near-white on dark, near-black on light) with zero new tokens. Highest conversion-impact single change.
#10 [high/moderate] Add shared .btn-primary / .btn-secondary classes and collapse the 7 button vocabularies onto them
     files: apps/web/app/tokens.css, apps/web/app/mcps/mcps.css, apps/web/app/settings/settings.css, apps/web/app/spaces/spaces.css, apps/web/app/features/[slug]/feature.css
     → Define in tokens.css (or a new global): .btn-primary { background:var(--cyan-soft); color:var(--cyan); border:1px solid var(--cyan-line); border-radius:10px; padding:9px 18px; font-weight:600; font-size:14px; cursor:pointer; transition:filter .13s,opacity .13s } .btn-primary:hover:not([disabled]){filter:brightness(1.08)} .btn-primary[disabled]{opacity:.5;cursor:not-allowed}. .btn-secondary mirrors with var(--panel)/var(--ink)/var(--border) and :hover{border-color:var(--border-strong)}. Replace .settings-save, .settings-btn, .connector-cta .primary-btn, .tool-panel .run-btn, .sp-new-btn, .feat-btn-primary (and ghost equivalents .connector-cta .ghost-btn, .feat-btn-secondary) with these classes. Eliminates the radius (8/10/12px) and padding divergence and adds the missing hover/transition that .settings-save, .run-btn, .feat-btn-primary lack. Keep auth-btn as its own solid variant per #9.
#11 [medium/safe] Add shared .alert-box (role=alert) and replace the 6 divergent inline/class error boxes
     files: apps/web/app/tokens.css, apps/web/app/components/ErrorBoundary.tsx, apps/web/app/mcps/RemoteMcpSection.tsx, apps/web/app/mcps/[id]/page.tsx, apps/web/app/billing/page.tsx
     → After #4, define one .alert-box { border-radius:10px; padding:10px 14px; font-size:13px; border:1px solid var(--error-line); background:var(--error-soft); color:var(--error) } and apply it (with role='alert') in place of the inline error divs in RemoteMcpSection.tsx L132, billing/page.tsx, mcps/[id]/page.tsx, and ErrorBoundary.tsx. Collapse .feat-err/.settings-err/.tool-panel .err/.admin-blocked to extend it. Fixes both the duplication and the missing role='alert' a11y gap on inline errors. Depends on #1,#4.
#12 [high/moderate] Replace the entirely-inline send/stop button styling with data-state CSS classes
     files: apps/web/app/dashboard.css, apps/web/app/page.tsx, apps/web/app/spaces/[id]/SpaceChat.tsx, apps/web/app/features/[slug]/FeatureChat.tsx
     → The send button's full visual state is an 8-property inline style object (page.tsx L951-960, SpaceChat L422-432, FeatureChat L753-766), which also escapes prefers-reduced-motion. Add to dashboard.css: .cmpr-send extends .cmpr-icon { width:auto; padding:0 10px } with .cmpr-send[data-state='idle']{color:var(--muted);background:var(--panel);opacity:.6;cursor:not-allowed} .cmpr-send[data-state='ready']{color:var(--cyan);background:var(--cyan-soft);border-color:var(--cyan-line)} .cmpr-send[data-state='sending']{color:var(--error);background:var(--error-soft);border-color:var(--error-line)} (note: --rose is a real token so the current var(--rose,...) fallback never fired — the inline approach itself is the problem). In each TSX set className='cmpr-icon cmpr-send' data-state={sending?'sending':value.trim()?'ready':'idle'} and delete the style prop. Pair with #7 for the glyph swap.
#13 [medium/moderate] Add a spacing scale + content-width tokens; fix the route-to-route max-width jump
     files: apps/web/app/tokens.css, apps/web/app/dashboard.css, apps/web/app/features/[slug]/feature.css, apps/web/app/spaces/spaces.css, apps/web/app/mcps/mcps.css, apps/web/app/settings/settings.css, apps/web/app/admin/admin.css
     → Add to tokens.css --sp-1:4px…--sp-8:32px,--sp-12:48px,--sp-16:64px and --content-prose:760px + --content-wide:1120px. Replace the 8 divergent page max-widths so chat/space-home/sp-gate use --content-prose and mcps(1200)/spaces-index(1100)/settings(1080)/admin(1280)/gallery(1100)/feat-shell(1180) all use --content-wide — removes the jarring measure change on navigation. Align spaces.css .sp-chat-col composer/transcript (720px) to --content-prose. Begin snapping the highest-traffic gaps/padding (transcript gap 18px, shell paddings) to the ramp. Token addition is safe; the width unification is the visible win.
#14 [medium/safe] Give the transcript real inter-turn breathing room
     files: apps/web/app/dashboard.css
     → Replace the single .transcript{gap:18px} (L475) and the fragile .msg-assistant + .msg-assistant{margin-top:-6px} hack (L509). Set .transcript{gap:0}; add .msg-row + .msg-row{margin-top:14px} for same-role adjacency and .msg-row-user + .msg-assistant,.msg-assistant + .msg-row-user-ish boundary {margin-top:32px} so conversation rounds read as distinct units (Claude/Gemini pattern). Also symmetrize the head/tail padding: .stage[data-chat=true] > .stage-inner padding-block 32px 16px → 32px (L445).
#15 [medium/safe] Lift the user message bubble off the ambient panel surface
     files: apps/web/app/dashboard.css
     → .msg-user (L481-495) uses var(--panel)+var(--border) — identical to sidebar rows, tool rows and popovers, so the user's own words have no identity. Change background to var(--surface-2) and border-color to var(--border-strong) (both tokens already exist), and bump padding 10px 18px → 12px 20px. One elevation step up, no new color.
#16 [high/safe] Add brand presence + ambient treatment to the auth pages
     files: apps/web/app/(auth)/auth.css, apps/web/app/(auth)/login/page.tsx, apps/web/app/(auth)/signup/page.tsx, apps/web/app/(auth)/forgot-password/page.tsx, apps/web/app/(auth)/reset-password/page.tsx
     → Auth is a brand void (plain card on blank field, product name never shown). In each auth page add before the h1: <div className='auth-brand'><img src='/pervagans-icon.png' className='auth-brand-icon'/></div>. In auth.css add .auth-brand{display:flex;justify-content:center;margin-bottom:4px} .auth-brand-icon{width:40px;height:40px;border-radius:10px}. Add a faint anchor: .auth-shell{background:radial-gradient(ellipse 600px 400px at 50% 40%, color-mix(in srgb,var(--cyan) 5%,transparent),transparent 70%)}. Reuses an existing asset, no new work.
#17 [medium/safe] Soften the over-engineered auth field labels + tighten the serif h1
     files: apps/web/app/(auth)/auth.css
     → .auth-field label (L31) is 11px all-caps mono at 0.16em — clashes with the serif h1. Change to font-family:var(--sans); font-size:12px; letter-spacing:.01em; text-transform:none; font-weight:500. Also .auth-card h1 (L19-24) has no line-height: set font-size:clamp(24px,5vw,32px); line-height:1.2; letter-spacing:-0.02em to prevent clipping on Arabic/long strings and tighten Instrument Serif at display size.
#18 [medium/safe] Make page-level headings render the Arabic serif in RTL + lift Arabic line-height
     files: apps/web/app/tokens.css, apps/web/app/dashboard.css
     → tokens.css only applies --ar-serif to .greet/.brand-greet (L87-88); every other font-family:var(--serif) heading (settings/mcps/admin/auth h1, .mcps-section-title, .connector-meta h1, .mcp-detail h1, .admin-stat .val) renders Latin Instrument Serif in RTL → Arabic falls back to Georgia. Add [dir='rtl'] h1, [dir='rtl'] .mcps-section-title, [dir='rtl'] .connector-meta h1, [dir='rtl'] .mcp-detail h1, [dir='rtl'] .admin-stat .val { font-family: var(--ar-serif) }. Also add [dir='rtl'] .md-body, [dir='rtl'] .msg-assistant { line-height:1.95 } and [dir='rtl'] .composer textarea{line-height:1.8} to stop Arabic ascender clipping. (Also add Amiri bold weight per #25.)
#19 [medium/safe] Fix the RTL expanded-chevron transforms that clobber the scaleX flip
     files: apps/web/app/dashboard.css
     → The expanded-state rules overwrite the base scaleX(-1) with only rotate(90deg), so RTL collapsed-section arrows point the wrong way. Change L160 to [dir='rtl'] .sb-features[data-expanded='true'] .sb-features-chev{transform:scaleX(-1) rotate(90deg)} and L177 to [dir='rtl'] .sb-sec-toggle[aria-expanded='true'] .sb-sec-chev{transform:scaleX(-1) rotate(90deg)}. Two-line, zero side effects.
#20 [medium/safe] Replace physical/hardcoded values: padding-right, nav-progress left, backdrop scrims, modal shadow, canvas white
     files: apps/web/app/mcps/mcps.css, apps/web/app/tokens.css, apps/web/app/dashboard.css, apps/web/app/components/CanvasDrawer.tsx
     → Bundle of logical-property/token fixes: (a) mcps.css L179 padding-right:44px → padding-inline-end:44px (RTL card text/button overlap). (b) tokens.css L119 .nav-progress left:0 → inset-inline-start:0. (c) dashboard.css .modal-backdrop L975 and .canvas-backdrop L914 hardcoded rgba(0,0,0,...) → background:var(--scrim) (light-mode parity; depends on #1). (d) .modal-card L984 box-shadow hardcoded 0.45 → box-shadow:var(--shadow). (e) .canvas-frame L931 background:#fff → var(--bg), and in CanvasDrawer buildSrcDoc replace injected background:#fff;color:#111 with background:canvas;color:canvastext so artifacts respect dark mode.
#21 [medium/safe] Restore a proper type ramp on assistant markdown headings + base line-height
     files: apps/web/app/tokens.css, apps/web/app/dashboard.css
     → md-body headings are compressed (h1 22 / h2 19 / h3 16.5 vs 15.5 body — h3 is +1px, invisible) and share one margin. Split per-level: h1 24px/700/margin-top 24px, h2 20px/650/20px, h3 17px/600/16px, h4 15px/600/uppercase. Add letter-spacing:-.015em to h1/h2. Add body{line-height:1.6} (currently unset → browser ~1.2). Optionally seed a --text-* ramp in tokens.css for future use; the heading fix is the visible win.
#22 [medium/moderate] Turn the brand wordmark into an editorial serif moment (hero + sidebar)
     files: apps/web/app/dashboard.css
     → The hero name uses sans-bold even though .greet is serif: .brand-greet .b1 (L325) overrides font-family:var(--sans);font-weight:700 — remove both so it inherits var(--serif) at weight 400, set font-size 48px and font-style italic. Mirror on the sidebar: .sb-brand .b1 (L45) → font-family:var(--serif);font-style:italic;font-weight:400;font-size:19px. Optional signature accent: add background:linear-gradient(135deg,var(--cyan),var(--purple));-webkit-background-clip:text;-webkit-text-fill-color:transparent to .brand-greet .b1 for the one hero color moment the product currently lacks. RTL ar-serif override already handles the hero.
#23 [medium/moderate] Differentiate composer active-context chips by type instead of all-cyan
     files: apps/web/app/page.tsx, apps/web/app/dashboard.css
     → Space, connector, web-search, deep-research and agent chips are all rendered as .model-pill with the same inline {background:var(--cyan-soft);color:var(--cyan);borderColor:var(--cyan-line)} (8+ duplicated blocks). Add a single .cmpr-chip class driven by a --hue var: background:color-mix(in srgb,var(--hue) 10%,transparent);color:var(--hue);border:1px solid color-mix(in srgb,var(--hue) 32%,transparent). Assign connectors --hue:var(--purple), spaces --hue:var(--yellow), deep-research --hue:var(--indigo), agent --hue:var(--fuchsia), web-search keeps --cyan. Remove the inline blocks from page.tsx. Gives the active-context row a legible type hierarchy.
#24 [medium/safe] Add gallery card interactivity, mobile grid clamp, and the missing empty-state hue
     files: apps/web/app/dashboard.css, apps/web/app/gallery/page.tsx
     → .gal-card (L875) has no hover and .gallery-page never sets --sp-hue-* so the empty-state icon tile is transparent. Add .gal-card{transition:border-color .15s,box-shadow .15s} .gal-card:hover{border-color:var(--border-strong);box-shadow:var(--shadow)} .gal-img{transition:transform .2s} .gal-media:hover .gal-img{transform:scale(1.03)}. Add --sp-hue-soft:var(--purple-soft);--sp-hue-line:var(--purple-line);--sp-hue:var(--purple) on .gallery-page and a padding-top:32px. Also add aspect-ratio:1/1 to .media-grid-item (L860) so batch image grids are uniform regardless of output dimensions.
#25 [medium/safe] Cap the Spaces rail width, add the unused subtitle, fix card-foot contrast, add focus ring
     files: apps/web/app/spaces/spaces.css, apps/web/app/spaces/page.tsx, apps/web/app/lib (UI strings)
     → (a) .sp-rail (L477-478) flex:0 0 360px → flex:0 0 clamp(300px,28%,400px); width:clamp(300px,28%,400px) so it isn't lopsided at 1440px+. (b) Render the defined-but-unused .spaces-sub: add <p className='spaces-sub'>{s.spacesSubtitle}</p> after the h1 (add the spacesSubtitle i18n key — it does not exist yet). (c) .sp-card-foot (L170) color var(--muted-2)→var(--muted) to clear WCAG AA on the timestamp. (d) Add .sp-instr-preview:focus-visible{outline:2px solid var(--sp-hue);outline-offset:2px;border-radius:10px} for keyboard access.
#26 [high/moderate] Preserve keyboard focus rings on all text inputs (remove bare outline:none)
     files: apps/web/app/tokens.css, apps/web/app/(auth)/auth.css, apps/web/app/mcps/mcps.css, apps/web/app/spaces/spaces.css, apps/web/app/settings/settings.css, apps/web/app/features/[slug]/feature.css, apps/web/app/dashboard.css
     → Every input/textarea/search uses outline:none with only a 32%-alpha border-color change as the focus cue, bypassing the global :focus-visible ring. Add one global rule in tokens.css: input:focus-visible,textarea:focus-visible,select:focus-visible{outline:2px solid var(--cyan);outline-offset:-1px;border-color:var(--cyan-line)} (negative offset keeps the ring inside the box, no layout shift). The existing per-field background/border focus styling stays as the mouse-hover affordance. Restores keyboard accessibility app-wide.
#27 [low/safe] Replace imperceptible/theme-breaking filter:brightness hovers with color-mix hovers
     files: apps/web/app/spaces/spaces.css, apps/web/app/features/[slug]/feature.css, apps/web/app/settings/settings.css
     → .sp-newthread-btn (spaces.css L290), .feat-subsb-new (feature.css L466) and .settings-btn (settings.css L240) hover via filter:brightness(1.05) — invisible on dark, washes pale cyan-soft toward white on light. Replace with the established recipe :hover{background:color-mix(in srgb,var(--cyan) 16%,transparent);border-color:var(--cyan)} (or var(--hue-ink) inside feature scope). Perceptible and theme-correct in both modes.
#28 [low/safe] Add hover/transition to admin tabs + settings nav (currently inert)
     files: apps/web/app/admin/admin.css, apps/web/app/settings/settings.css
     → .admin-tabs button (admin.css L22) has no :hover and no transition; .settings-nav button (settings.css L40) hover hard-flips with no transition. Add transition:color .15s,background .15s to both, and .admin-tabs button:hover{color:var(--ink-2);background:var(--panel)}. Makes both nav surfaces feel alive and consistent with the rest of the app.
#29 [medium/safe] Add missing responsive overrides: admin page, settings inner rows, dashboard popovers, msg-user width
     files: apps/web/app/admin/admin.css, apps/web/app/settings/settings.css, apps/web/app/dashboard.css
     → (a) admin.css has ZERO media queries: add @media(max-width:640px){.admin-shell{padding:16px 14px 64px} .admin-shell h1{font-size:28px} .admin-toolbar{flex-direction:column;align-items:stretch} .admin-toolbar input[type=search]{min-width:0;width:100%}}. (b) settings.css inside the existing @media(max-width:720px) add .settings-row{grid-template-columns:1fr} .settings-row .input,select,textarea{min-width:0;width:100%} (currently min-width:240px overflows phones). (c) dashboard.css add @media(max-width:560px){.model-pop,.popover{min-width:0;width:min(300px,calc(100vw - 28px))}} (the feature-page fix never covered the dashboard popovers) and .msg-user{max-width:88%}.
#30 [medium/safe] Lighten the brand-image loading mark + fix ConnectorIcon white backdrop
     files: apps/web/app/dashboard.css, apps/web/app/page.tsx, apps/web/app/components/ConnectorIcon.tsx
     → (a) The loading state renders a 40x40 brand PNG (page.tsx) pulsing next to the dots — dominant and blurs on HiDPI. Reduce .msg-loading-mark to 20x20 in dashboard.css and the img width/height={40}→{20}; soften @keyframes brand-pulse scale to 0.95–1.0. (b) ConnectorIcon.tsx L251 background:'#ffffff' → 'var(--panel-solid)' (or 'var(--surface-2)') so dark-logo connectors don't sit on a blinding white square in dark mode; bump padding to Math.max(4,Math.round(size*0.10)).
#31 [low/safe] Replace typographic × close glyphs with the SVG X + give modal-close a focus ring
     files: apps/web/app/components/Modal.tsx, apps/web/app/components/CanvasDrawer.tsx, apps/web/app/dashboard.css
     → Modal.tsx and CanvasDrawer.tsx render a raw × (font-size 22px vs 16px — inconsistent across OS/zoom). Replace both with a 16x16 SVG X (two diagonal lines, stroke currentColor, strokeWidth 1.75, round caps); the now-irrelevant font-size rules on .modal-close/.canvas-close can be removed. Add .modal-close:focus-visible{outline-offset:3px;border-radius:8px} so the ring isn't clipped by the tight header padding. Add a real enter animation to .canvas-drawer (animation:canvas-slide-in .22s; @keyframes from{transform:translateX(100%)}) so it stops snapping open.
#32 [low/safe] Remove redundant popover-row inline styles + dead .md-code-copy CSS
     files: apps/web/app/page.tsx, apps/web/app/dashboard.css
     → (a) The Add-popover buttons carry style={{width:'100%',textAlign:'start',border:0,background:'transparent',cursor:'pointer'}} that duplicate .popover-row and the border:0 wrongly defeats the :hover border. Remove the inline styles. (b) Delete the dead first .md-code-copy block (dashboard.css L677-699) left over from the code-block refactor — it positions absolute against an element that no longer exists as a direct child; the live rules are the .md-code-actions block (L902-911). Also change .md-code-canvas:hover color:var(--ink) (washes out the accent) to opacity:.85.
#33 [low/safe] Promote --surface-2 for the citation JSON panel + align section-label idiom + blockquote tint
     files: apps/web/app/dashboard.css
     → (a) .msg-citation-detail (L641) background var(--bg-2) is near-pure-black in dark — change to var(--surface-2) (the unused token designed for exactly this elevated-readable surface) and border-color:var(--border-strong). (b) .msg-sources-label (L767) diverges (11px/700/0.04em sans) from every other section label — change to font-family:var(--mono);font-size:10px;letter-spacing:0.16em;color:var(--muted-2) to match .msg-citations-label. (c) .md-body blockquote (L558) add background:var(--cyan-soft);border-radius:0 8px 8px 0;padding:6px 14px so it visibly reads as a quote. (d) Add .md-body > table{display:block;overflow-x:auto} to stop wide-table overflow on mobile.
#34 [low/safe] Fix iOS/PWA brand seams: themeColor mismatch + missing apple-touch-icon
     files: apps/web/app/layout.tsx, apps/web/public/manifest.webmanifest, apps/web/public/apple-touch-icon.png
     → (a) layout.tsx L95 themeColor '#0a0a0c' ≠ the real --bg #0a0c10 → visible OLED chrome seam; change to '#0a0c10' and match background_color/theme_color in manifest.webmanifest. (b) No apple-touch-icon.png exists (verified) → iOS home-screen shows a blurry crop. Add apple:'/apple-touch-icon.png' to layout metadata icons and ship a 180x180 PNG (pad pervagans-icon to square). NOTE: do NOT add display=swap to the font URL — it is already present (audit claim was stale).
#35 [medium/moderate] Extract the all-inline billing page into billing.css using the shared tokens/classes
     files: apps/web/app/billing/page.tsx, apps/web/app/billing/billing.css, apps/web/app/billing/cancel/page.tsx, apps/web/app/billing/return/page.tsx
     → billing/page.tsx is 100% inline styles (card radius 18 vs the system's 14, h1 48 vs 44, a runtime btnStyle() instead of a class) — the single most off-system page. Create billing.css with .billing-shell (mirror .settings-shell, max-width var(--content-wide)), .billing-card (radius 14, var(--panel-solid)+border+var(--shadow)), .billing-price (serif), and use .btn-primary (#10) for CTAs; mark the Pro plan .is-featured{border-color:var(--cyan-line);box-shadow:0 0 0 1px var(--cyan-line),var(--shadow)} with a 'Most popular' badge and a filled cyan CTA. Replace the ✅ emoji in return/page.tsx with an SVG check and add the missing Arabic locale branch to cancel/page.tsx. Largest single consistency cleanup.

=== CRITIQUE ===
{
  "grounded": true,
  "verdict": "GROUNDED / SHIP-WORTHY with minor edits. I spot-checked ~25 of the 35 fixes at line level against the real source in apps/web/app (tokens.css, dashboard.css, all 6 per-surface CSS files, icons.tsx, RemoteMcpSection.tsx, page.tsx, login/page.tsx, spaces/page.tsx, layout.tsx, CanvasDrawer/ConnectorIcon/Modal) — far exceeding the ≥8 bar. Citations are accurate to within ~1 line and the described code states are real. The central thesis (a missing token MIDDLE LAYER drives the duplication) is independently verifiable: --surface-2 is genuinely unused outside tokens.css, --orange/--green genuinely lack the -soft/-line derivatives that indigo/rose/fuchsia have, the raw-literal neon data-color blocks (dashboard.css L128-137 + L1029-1033, feature.css L30-39 + L749) are real and sit OUTSIDE the [data-theme] blocks so they cannot theme-switch, and the light-mode contrast failures check out numerically (--cyan #0a8fa6 = 3.72:1, --green = 3.52:1, --orange = 3.04:1 on bg, all failing AA for normal text; proposed #0079a0 = 4.82:1). Two factual defects to correct before execution (see corrections) and a few subjective/aesthetic items to downgrade (see overreach). Nothing in the plan is dangerous; the token additions are correctly characterized as additive/zero-regression.",
  "corrections": [
    "Fix #2 (data-connected) — SYMPTOM MISDESCRIBED, fix still correct. The plan says the Connect and Disconnect buttons 'render identically.' They do NOT: both use className 'connect-btn' inside a '.mcp-card' div, and mcps.css L203-205 (.mcp-card .connect-btn[data-connected=\"true\"]) applies cyan. The UNCONNECTED Connect button (RemoteMcpSection.tsx L113) carries a bare data-connected (→ data-connected=\"true\" in the DOM) so it gets the cyan ACTIVE style, while the CONNECTED Disconnect button (L102-109) has no such attr and renders plain. The real bug is an INVERSION (the not-yet-connected action looks connected), not identical rendering. Removing the bare attribute at L113 is the right fix; just rewrite the rationale.",
    "Fix #32b (delete 'dead' .md-code-copy) — RATIONALE WRONG, action low-value. There ARE two .md-code-copy definitions (dashboard.css L677-699 standalone position:absolute, and L903 inside .md-code-actions forced position:static). The live JSX uses the .md-code-actions wrapper, so the standalone L677-699 block is SUPERSEDED — but the plan's stated reason ('positions absolute against an element that no longer exists as a direct child') is false: .md-code is explicitly re-declared at L901 and still exists. Before deleting, a dev must confirm the live markup never renders a bare .md-code-copy outside .md-code-actions; the plan asserts this with unwarranted confidence.",
    "Fix #4 / #11 wording — several 'error boxes' use color-mix(in srgb, var(--purple) 12%, transparent) for their BACKGROUND, not var(--purple-soft): auth.css .auth-err L56, mcps.css .tool-panel .err L378. The plan's instruction to 'replace var(--purple-soft)' is slightly imprecise for those two — the swap target is the color-mix expression (→ var(--error) 12%) plus the --purple-line/--purple. Minor, but a literal find/replace of '--purple-soft' would miss them.",
    "Fix #23 overcounts: the plan says '8+ duplicated' inline-cyan model-pill chip blocks in page.tsx; I count 6 (L783, L798, L806, L816, L827, L838). The claim is directionally correct (all chips are the same cyan with no type hierarchy) but the number is inflated.",
    "Fix #2/#3 contrast figures are rounded slightly optimistically: plan cites --cyan 'at 3.8:1' and green 'at 1.7:1'. Measured on the real light --bg (#fbfcfe): --cyan #0a8fa6 = 3.72:1; the raw neon green rgb(74,222,128) the plan calls '1.7:1' actually lands ~1.6:1 on white — close enough that the conclusion (fails AA) holds, but the exact numbers are approximate."
  ],
  "gaps": [
    "No fix addresses the hardcoded WHITE/near-black values inside the brand assets themselves: icons.tsx discLogo (L201-206) hardcodes border 'rgba(255,255,255,0.82)' + box-shadow 'rgba(0,0,0,0.55)/rgba(86,207,225,0.32)' as inline styles, and the .sb-account .av gradient uses literal 'color: white' (dashboard.css L252). These are the same raw-literal anti-pattern the plan crusades against elsewhere but are never collected.",
    "The plan tokenizes scrims/shadows but misses .gal-badge (dashboard.css L886 background rgba(0,0,0,.55); color:#fff) — a hardcoded dark scrim chip that won't adapt and duplicates the very --scrim token fix #1 introduces.",
    "Composer/textarea base line-height and font sizing: .composer textarea is 16px/1.5 (dashboard.css L333) while .msg-user is 14.5px and .msg-assistant 15.5px — the input text is LARGER than the rendered conversation, a real visual mismatch no fix touches (fix #13/#21 reshape transcript + headings but not the input).",
    "Z-index ladder is ad hoc and uncollected: nav-progress 9999, modal-backdrop 1000, canvas 60/61, popovers 30, mobile drawer 54/60 — there is no token/scale for stacking, and fix #13's 'tokens for everything repeated' spirit skips it. Minor, but it's a real magic-number family.",
    "Focus-ring color is hardwired to --cyan everywhere (tokens.css L94 global, plus fix #26's proposed input rule). On feature pages the accent is --hue-ink (orange/green/etc.), so a keyboard focus ring will clash with the surrounding hue. The plan's own per-surface hue system argues the focus ring should use var(--hue-ink, var(--cyan)); fix #26 doesn't account for this.",
    "Accessibility: the icon-only buttons that get NEW glyphs (send/stop/lock/dice in fix #5/#7) already have aria-labels in page.tsx (L945) — good — but the plan never checks that FeatureChat/SpaceChat send buttons carry the same aria-label/title, and swapping a text glyph ('↵'/'■') for an SVG removes the only accessible text if the label is missing on those two surfaces. Worth verifying during #7."
  ],
  "overreach": [
    "Fix #22 (editorial serif wordmark + cyan→purple gradient text-fill on the hero name) is the most subjective item and the riskiest aesthetically. Changing .brand-greet .b1 (verified at dashboard.css L325 as sans/700/44px) to italic serif 48px with a gradient -webkit-text-fill is a strong brand-identity bet, not a correctness fix, and gradient text has its own legibility/clipping risks. Downgrade to optional/proposal; do not bundle with the safe token work.",
    "Fix #14's specific spacing values (same-role +14px, cross-role +32px, gap:0) are invented design numbers presented as if derived. The underlying problem (the .msg-assistant + .msg-assistant{margin-top:-6px} hack at L509 is fragile) is real and verified, but the exact ramp is a judgment call — frame it as a starting point, not a spec, and note the cross-role selector '.msg-row-user-ish' in the plan is a placeholder that doesn't exist as a real class.",
    "Fix #25b (render the orphaned .spaces-sub with a NEW spacesSubtitle i18n key) adds net-new user-facing copy in two locales — that's product/content scope, not a CSS consistency fix. Verified the class exists (spaces.css L51) and the key does not, but inventing marketing subtitle text is out of a 'fix plan' remit; cut or hand to product.",
    "Fix #9's claim that a solid auth CTA is 'highest conversion-impact single change' is an unsupported business assertion. The CSS change (.auth-btn → ink/bg solid, verified ghost-tint at auth.css L44-53) is fine and safe; the conversion claim should be dropped — there's no measurement behind it.",
    "Fix #23/#27 lean heavily on color-mix(in srgb, …) for the new chip/hover recipes. color-mix is well-supported now but is a newer feature than the rest of the codebase's rgba() idiom; given the app already ships rgba literals, introducing color-mix as the standard for NEW shared classes is a mild consistency risk worth a conscious decision rather than an assumed default. (Note the codebase DOES already use color-mix in body::before and a few hovers, so this is minor.)",
    "Fix #1 proposes a full spacing ramp (--sp-1..--sp-16) AND a type ramp AND content-width tokens all under one 'high/safe' item, but fixes #13/#21 admit the actual snapping of values is the work. Adding unused tokens is safe but the plan slightly oversells #1's blast radius — most of the spacing/type payoff is deferred to #13/#21, which are correctly marked 'moderate'. Keep #1 scoped to colors/scrim (the genuinely cascading wins) and treat the ramps as seeds."
  ]
}
