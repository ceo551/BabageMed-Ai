<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions the WebView needs to surface a feature-rich experience.
         Camera/Mic/Location are only requested at runtime when the page
         actually invokes them (handled by ContextActivity's permission
         launcher). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <queries>
        <!-- App linking — let the system know we participate in the
             default browser intent so users can pick the app for
             babagemed.com links. -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:name=".PervagansApp"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.PervagansAI"
        android:usesCleartextTraffic="false"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden|uiMode"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.PervagansAI">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- App Links: open app-routable paths under https://babagemed.com
                 in the app when installed. Requires /.well-known/assetlinks.json
                 published at the babagemed.com root with this app's SHA-256
                 fingerprint — see README.
                 Restrict to explicit pathPrefix entries so we don't hijack
                 marketing pages or unrelated URLs. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="babagemed.com" android:pathPrefix="/c/" />
                <data android:scheme="https" android:host="babagemed.com" android:pathPrefix="/features/" />
                <data android:scheme="https" android:host="babagemed.com" android:pathPrefix="/mcps" />
                <data android:scheme="https" android:host="babagemed.com" android:pathPrefix="/settings" />
                <data android:scheme="https" android:host="babagemed.com" android:pathPrefix="/billing" />
            </intent-filter>
        </activity>
    </application>
</manifest>
