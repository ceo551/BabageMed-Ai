# ArgoCD AppProject — scopes what the Pervagans application can deploy.
# Apply once: `kubectl apply -f infra/gitops/argocd/project.yaml -n argocd`
apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: pervagans
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  description: Pervagans AI — Next.js + Go + Postgres + 262 MCPs
  sourceRepos:
    # Pin to your fork / private mirror in production.
    - https://github.com/ceo551/pervagans-ai
    - https://github.com/ceo551/pervagans-ai.git
  destinations:
    - namespace: pervagans
      server: https://kubernetes.default.svc
    - namespace: pervagans-staging
      server: https://kubernetes.default.svc
  clusterResourceWhitelist:
    - group: "*"
      kind: "Namespace"
    - group: "rbac.authorization.k8s.io"
      kind: "*"
  namespaceResourceWhitelist:
    - group: "*"
      kind: "*"
  orphanedResources:
    warn: true
